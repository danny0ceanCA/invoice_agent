export function VendorLanding() {
  return (
    <div className="space-y-4">
      <h2 className="text-lg font-semibold">Vendor Workspace</h2>
      <p className="text-sm text-slate-600">
        Upload timesheets, monitor processing jobs, and download the generated invoice
        artifacts. The production build will integrate Auth0 authentication and real-time
        job status updates from Redis.
      </p>
    </div>
  );
}
