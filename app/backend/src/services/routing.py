"""Workflow routing utilities."""

from __future__ import annotations


def mark_pending(invoice_id: int) -> None:
    """Placeholder routing action."""
    print(f"Invoice {invoice_id} marked as pending review")
